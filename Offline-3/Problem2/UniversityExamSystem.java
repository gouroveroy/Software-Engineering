public class UniversityExamSystem {
    public static void main(String[] args) {
        // Mediator examController = new ExamControllerOffice();

        // // Create Students
        // Student student1 = new Student("S101", "Alice", examController);
        // Student student2 = new Student("S102", "Bob", examController);

        // // Create Teachers
        // Teacher teacher1 = new Teacher("T101" ,"Dr. Smith", examController);
        // Teacher teacher2 = new Teacher("T102" ,"Dr. Brown", examController);

        // // Examiner submits marks
        // examController.submitScriptsAndMarks("Dr. Smith", "Math", 85, student1.getId());
        // examController.submitScriptsAndMarks("Dr. Brown", "Physics", 78, student2.getId());

        // // Students view their results
        // student1.viewResult();
        // student2.viewResult();

        // // Student requests re-examination
        // student1.requestReExamination("Math");

        // // Teacher re-examines and updates marks
        // teacher1.reExamineScript(student1.getId(), "Math", 90);

        // // Students view updated results
        // student1.viewResult();

        // // Student requests re-examination
        // student2.requestReExamination("Physics");

        // // Teacher re-examines and updates marks
        // teacher2.reExamineScript(student2.getId(), "Physics", 85);

        // // Students view updated results
        // student2.viewResult();
    }
}
